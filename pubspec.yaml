name: hatchway_flutter
description: Flutter SDK for Hatchway — deployment control, feature flags, and remote config for mobile apps.
version: 0.1.0
homepage: https://hatchway.dev
repository: https://github.com/kennedyowusu/hatchway_flutter

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  flutter_secure_storage: ^9.0.0
  shared_preferences: ^2.2.0
  package_info_plus: ^8.0.0
  crypto: ^3.0.3
  uuid: ^4.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  # No assets or fonts needed for SDK
