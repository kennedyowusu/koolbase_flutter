name: koolbase_flutter
description: Flutter SDK for Koolbase — feature flags, remote config, version enforcement, authentication, file storage, database, and realtime subscriptions for mobile apps.
version: 1.4.0
homepage: https://koolbase.com
repository: https://github.com/kennedyowusu/koolbase_flutter

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  flutter_secure_storage: ^9.0.0
  shared_preferences: ^2.2.0
  package_info_plus: ^8.0.0
  crypto: ^3.0.3
  uuid: ^4.4.0
  web_socket_channel: ^3.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  # No assets or fonts needed for SDK
